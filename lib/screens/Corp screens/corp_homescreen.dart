import 'package:flutter/material.dart';

class CorpHomescreen extends StatelessWidget {
  const CorpHomescreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Corporate user homescreen")),
    );
  }
}
